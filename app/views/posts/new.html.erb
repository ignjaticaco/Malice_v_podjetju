<h1 class='mt-3'>Nova malica</h1>
<%= form_for :post, url: posts_path do |f| %>
<% if @post.errors.any? %>
  <% @post.errors.full_messages.each do |msg| %>
    <div class='alert alert-danger'><%= msg %></div>
  <% end %>
<% end %>
  <p>Tudi ta stran vidna samo adminom ob dostopu do rails c (v views/layouts ZA ADMINE
if current_admin.admin?)</p>
  <p>
    <%= f.label :Naslov %><br>
    <%= f.text_field(:title, {:class => 'form-control'}) %>
  </p>
 
  <p>
    <%= f.label :Opis %><br>
    <%= f.text_area(:body, {:class => 'form-control'}) %>
  </p>

  <p>
    <%= f.label :Datum %><br>
    <%= f.text_field(:date, {:class => 'form-control'}) %>
  </p>

  <p>
    <%= f.submit({:class => 'btn btn-md btn-outline-primary'}) %>
  </p>
<% end %>